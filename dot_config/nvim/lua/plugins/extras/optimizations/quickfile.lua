return {
	"folke/snacks.nvim",
	opts = {
		quickfile = {
			-- The setup function will be called with the config option
			setup = function(config)
				-- Use the config to setup your plugin
			end,
		},
	},
}
